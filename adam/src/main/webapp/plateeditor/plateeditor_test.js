'use strict';

describe('hesAdam.plateeditor module', function() {

  beforeEach(module('hesAdam.plateeditor'));

  describe('plateeditor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var plateeditorCtrl = $controller('PlateeditorCtrl');
      expect(plateeditorCtrl).toBeDefined();
    }));

  });
});